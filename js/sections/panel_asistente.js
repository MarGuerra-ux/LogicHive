// ================================================
// PANEL_ASISTENTE.JS
// Placeholder — se desarrolla en fase posterior
// ================================================

function initAsistente() {
  const wrap = document.getElementById("asistenteWrap");
  if (!wrap) return;
  wrap.innerHTML = `
    <div style="text-align:center;padding:48px 24px">
      <p style="font-size:32px;margin:0 0 12px">🤖</p>
      <h3 style="margin:0 0 8px">Asistente IA</h3>
      <p class="muted-text">Próximamente — generá código, resumí apuntes y creá diagramas con IA.</p>
    </div>`;
}
