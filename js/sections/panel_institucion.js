// ================================================
// PANEL_INSTITUCION.JS
// Placeholder — se desarrolla en fase posterior
// ================================================

function initInstitucion() {
  const wrap = document.getElementById("institucionWrap");
  if (!wrap) return;
  wrap.innerHTML = `
    <div style="text-align:center;padding:48px 24px">
      <p style="font-size:32px;margin:0 0 12px">🏛️</p>
      <h3 style="margin:0 0 8px">Institución</h3>
      <p class="muted-text">Próximamente — agregá tu universidad, carrera y sección para recibir contenido específico.</p>
    </div>`;
}
